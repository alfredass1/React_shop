import React from 'react';
import Navigation from "../nav/Nav";

function header() {
    return(
        <div className="container">
            <Navigation/>
        </div>

    );

}

export default header;